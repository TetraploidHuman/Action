---
description: Cursor Remote-SSH 连接远端 ~/Action 开发与测试
alwaysApply: true
---

# 远程开发工作流（Cursor Remote-SSH）

## 核心原则

通过 **Cursor Remote-SSH** 直接打开远端 `~/Action`，在本机 Cursor 里编辑、编译、测试、提交——**工作区即远端，无需本地镜像或 paramiko 同步**。

```
本机 Cursor ──Remote-SSH──► 远端 ~/Action（唯一工作区）
                                │
                    nix-shell → build / test / benchmark
                                │
                    git commit & push → GitHub
```

## 连接

### SSH 配置（`~/.ssh/config`）

```ssh-config
Host action-remote
    HostName ssh.tetraploid.cn
    Port 11025
    User miaox99
    IdentityFile ~/.ssh/id_ed25519
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

### Cursor 连接步骤

1. `Ctrl+Shift+P` → **Remote-SSH: Connect to Host...** → `action-remote`
2. **File → Open Folder** → `/home/miaox99/Action`
3. 左下角显示 **`SSH: action-remote`** 即已连上

### 环境信息

| 项 | 值 |
|----|-----|
| 系统 | NixOS x86_64 |
| 项目目录 | `/home/miaox99/Action` |
| 开发环境 | `nix-shell`（LLVM 21.1.8 + Rust） |
| GitHub | `https://github.com/TetraploidHuman/Action.git`，分支 `main` |

终端里先进入开发环境：

```bash
cd ~/Action
nix-shell
```

## 常用命令

```bash
cargo build --release
cargo test -- --test-threads=1
./target/release/action run examples/hello.at
./benchmark.sh --iterations 3
./benchmark.sh --mode aot -O2
python3 scripts/perf_phase_split.py
python3 scripts/perf_report.py
```

详见 `BENCHMARK.md` 与 `scripts/perf_*.py`。

## 标准开发流程

1. **Remote-SSH 连接** `action-remote`，打开 `/home/miaox99/Action`
2. **修改代码**（Cursor 编辑器直接改远端文件）
3. **验证**：`nix-shell` 内 `cargo build --release` + 测试 / benchmark
4. **提交**：
   ```bash
   git add ...
   git commit -m "..."
   ./scripts/git-push.sh origin main
   ```

## GitHub 访问（直连优先 / FlClash 备选）

全局 `git config` 若设置了 `http.proxy=127.0.0.1:7890`，会导致 git 走 FlClash；节点异常时会 push 失败。

**推荐 push 方式**（绕过全局代理，优先直连）：

```bash
./scripts/git-push.sh origin main
# 等价于先尝试：
git -c http.proxy= -c https.proxy= push origin main
```

直连失败时脚本会自动回退到 FlClash `7890`。

## NixOS + Cursor Server 备忘

首次连接会下载 Cursor Server；若失败可尝试：

- Cursor 设置 `remote.SSH.connectTimeout`: `300`
- Cursor 设置 `remote.SSH.useLocalServer`: `false`
- 远端修复 bundled node：
  ```bash
  cd ~/.cursor-server/bin/*/
  rm -f node && ln -s "$(which node)" node
  ```

性能测试结果：`~/Action/benchmark_results.txt`
