// M139: heterogeneous Set elements in PlainBlock for-in (dual frontend).
fun main() Int {
    return if true {
        val s Set = Set[1, true]
        var sum Int = 0
        for x in s {
            sum = sum + x
        }
        sum
    } else {
        0
    }
}
