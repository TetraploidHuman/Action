// M171: assign Int to Point inside PlainBlock (dual frontend).
type Point = {x: Int, y: Int}

fun main() -> Int {
    return if true {
        var p: Point = {x = 0, y = 0}
        p = 1
        p.x
    } else {
        0
    }
}
