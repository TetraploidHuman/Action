// M173: call Point-param with Int inside PlainBlock (dual frontend).
type Point = {x: Int, y: Int}

fun sumX(p: Point) -> Int {
    return p.x
}

fun main() -> Int {
    return if true {
        sumX(1)
    } else {
        0
    }
}
