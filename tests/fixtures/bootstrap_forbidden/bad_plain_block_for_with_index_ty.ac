// M132: heterogeneous List in PlainBlock for-with-index (dual frontend).
fun main() Int {
    return if true {
        var sum Int = 0
        for idx, n in List[1, true] {
            sum = sum + n
        }
        sum
    } else {
        0
    }
}
