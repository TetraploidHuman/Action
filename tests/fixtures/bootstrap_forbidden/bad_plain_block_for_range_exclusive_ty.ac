// M155: exclusive range end not Int inside PlainBlock (dual frontend).
fun main() Int {
    return if true {
        var s Int = 0
        for i in 1..<true {
            s = s + 1
        }
        s
    } else {
        0
    }
}
