// M181: Point field Bool where Int expected inside PlainBlock (dual frontend).
type Point = {x: Int, y: Int}

fun main() -> Int {
    return if true {
        val p: Point = {x = true, y = 20}
        val tag: Int = if p.x == 10 { 1 } else { 0 }
        println(tag)
        0
    } else {
        1
    }
}
