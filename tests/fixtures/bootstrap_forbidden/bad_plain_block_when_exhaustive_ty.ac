// M146: non-exhaustive when inside PlainBlock (dual frontend).
enum Color { Red, Green, Blue }

fun main() Int {
    return if true {
        val c Color = Red
        when c {
            Red { 1 }
            Green { 2 }
        }
    } else {
        0
    }
}
