// M135: non-Bool if-cond around break inside PlainBlock for (dual frontend).
fun main() -> Int {
    return if true {
        var sum: Int = 0
        for i in 1..100 {
            if i { break } else { sum = sum + i }
        }
        sum
    } else {
        0
    }
}
