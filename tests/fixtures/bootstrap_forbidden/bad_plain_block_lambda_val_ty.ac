// M190: lambda body val init type error (call site in PlainBlock; dual frontend).
fun once() -> Int {
    return lambda { 0; val a: Int = true; a }()
}

fun main() -> Int {
    return if true {
        once()
    } else {
        0
    }
}
