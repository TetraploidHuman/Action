// M144: when guard not Bool inside PlainBlock (dual frontend).
enum Color { Red, Green, Blue }

fun main() Int {
    return if true {
        val c Color = Red
        when c {
            Red and 1 { 0 }
            else { 1 }
        }
    } else {
        0
    }
}
