// M189: multi-stmt lambda last expr type error (call site in PlainBlock; dual frontend).
fun once() -> Int {
    return { 21; 1 and true }()
}

fun main() -> Int {
    return if true {
        once()
    } else {
        0
    }
}
