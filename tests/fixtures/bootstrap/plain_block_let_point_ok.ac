// M172: PlainBlock Point let — mirror let_point_ok → 0.
type Point = {x: Int, y: Int}

fun origin() -> Point {
    return {x = 0, y = 0}
}

fun main() -> Int {
    return if true {
        val p: Point = origin()
        p.x
    } else {
        1
    }
}
