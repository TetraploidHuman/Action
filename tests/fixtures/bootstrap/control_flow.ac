fun main() Int {
    var sum Int = 0
    var i Int = 0
    for i < 10 {
        sum = sum + i
        i = i + 1
    }
    for n in List[1, 2, 3] {
        sum = sum + n
    }
    println(sum)
    return 0
}
