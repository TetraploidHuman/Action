// M163: PlainBlock inclusive range for — mirror for_range → 10.
fun main() -> Int {
    return if true {
        var sum: Int = 0
        for i in 1..5 {
            sum = sum + i
        }
        sum
    } else {
        0
    }
}
