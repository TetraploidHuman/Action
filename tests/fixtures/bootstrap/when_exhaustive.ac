enum Color { Red, Green, Blue }

fun main() Int {
    val c Color = Blue
    val code Int = when c {
        Red { 1 }
        Green { 2 }
        Blue { 3 }
    }
    println(code)
    return code
}
