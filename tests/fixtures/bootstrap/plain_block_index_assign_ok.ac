// M143: PlainBlock index assign — mirror index_assign_ok → 0.
fun main() -> Int {
    return if true {
        var xs: List = List[1, 2, 3]
        xs[0] = 9
        0
    } else {
        1
    }
}
