// M167: PlainBlock index key — mirror index_key_ok → 0.
fun main() -> Int {
    return if true {
        var xs: List = List[1, 2, 3]
        xs[0] = 9
        var m: Map = Map["a": 1]
        m["a"] = 2
        0
    } else {
        1
    }
}
