// M137: PlainBlock nested for-in — mirror nested_for → 3.
fun main() -> Int {
    return if true {
        var sum: Int = 0
        for i in 1..3 {
            for j in 1..2 {
                sum = sum + i * j
            }
        }
        sum
    } else {
        0
    }
}
