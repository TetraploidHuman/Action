// M138: PlainBlock Map for k, v (IterateWithIndex) — sum values → 15.
fun main() -> Int {
    return if true {
        val m: Map = Map["a": 10, "b": 5]
        var sum: Int = 0
        for k, v in m {
            sum = sum + v
        }
        sum
    } else {
        0
    }
}
