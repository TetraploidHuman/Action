// M146: PlainBlock when exhaustive — mirror when_exhaustive → 3.
enum Color { Red, Green, Blue }

fun main() -> Int {
    return if true {
        val c: Color = Blue
        when c {
            Red -> 1
            Green -> 2
            Blue -> 3
        }
    } else {
        0
    }
}
