// M190: PlainBlock lambda body mid-stmt val — mirror lambda_val_ok → 42.
// Call site in PlainBlock; `{ 21; val a: Int = 21; a * 2 }()` in helper.
fun once() -> Int {
    return { 21; val a: Int = 21; a * 2 }()
}

fun main() -> Int {
    return if true {
        once()
    } else {
        0
    }
}
