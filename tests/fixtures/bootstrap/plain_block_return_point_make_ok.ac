// M174: PlainBlock return Point make — mirror return_point_make → 0.
type Point {x Int, y Int}

fun origin() Point {
    return Point {x = 0, y = 0}
}

fun main() Int {
    return if true {
        val p Point = origin()
        p.x + p.y
    } else {
        1
    }
}
