// M132: PlainBlock for-with-index — sum List[1,2,3] → 6.
fun main() Int {
    return if true {
        var sum Int = 0
        for idx, n in List[1, 2, 3] {
            sum = sum + n
        }
        sum
    } else {
        0
    }
}
