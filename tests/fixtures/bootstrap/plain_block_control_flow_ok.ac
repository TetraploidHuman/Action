// M193: PlainBlock control flow — mirror control_flow → 0.
fun main() -> Int {
    return if true {
        var sum: Int = 0
        var i: Int = 0
        for i < 10 {
            sum = sum + i
            i = i + 1
        }
        for n in List[1, 2, 3] {
            sum = sum + n
        }
        println(sum)
        0
    } else {
        1
    }
}
