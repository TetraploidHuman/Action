// M189: PlainBlock multi-stmt brace lambda — mirror lambda_stmts_ok → 42.
// Call site in PlainBlock; `{ 21; 21 * 2 }()` lives in helper (if-arm brace disambiguation).
fun once() -> Int {
    return { 21; 21 * 2 }()
}

fun main() -> Int {
    return if true {
        once()
    } else {
        0
    }
}
