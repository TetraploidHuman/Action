// M173: PlainBlock Point call — mirror call_point_ok → 0.
type Point {x: Int, y: Int}

fun origin() -> Point {
    return Point {x = 0, y = 0}
}

fun sumX(p: Point) -> Int {
    return p.x
}

fun main() -> Int {
    return if true {
        sumX(origin())
    } else {
        1
    }
}
