// Test: callback returning Option
enum Option { Some(Int), None }

fun apply_direct(f: (Int) -> Option, x: Int) -> Option = f(x)

fun main() {
    val r1 = apply_direct({ x -> Some(x * 2) }, 21)
    when r1 {
        Some(v) -> print(v)
        None -> print(0)
    }
}