// Test: FFI - CString conversion
external fun puts(s: CString): Int

fun main() {
    val msg = "Hello from Atomic FFI!"
    val cstr = toCString(msg)
    val back = fromCString(cstr)
    println(back)
    println("done")
}
