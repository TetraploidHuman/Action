// Input/Output: readLine() returns Option<String> (None on EOF)
fun main() {
    val name_opt = readLine()
    val name = unwrapOr(name_opt, "World")
    val greeting = concat("Hello, ", name)
    println(greeting)
}