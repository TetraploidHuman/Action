// Test: struct with nullable field - comprehensive
fun main() {
    // Null case
    val p = {x = 10, age = null}
    print(p.x)
    print(p.age or { -1 })

    // Non-null case
    val p2 = {x = 20, age = 25}
    print(p2.x)
    print(p2.age or { -1 })
}
