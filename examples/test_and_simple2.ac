// Test: and guards in if - simple value match
fun main() { val x = 5
    val result = when x {
        n and n > 0 -> "positive"
        else -> "non-positive"
    }
    println(result)
    println("done") }