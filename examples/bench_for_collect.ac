fun main() {
    var lst = List[]; var i = 0
    for i < 2000 { lst = lst.append(i); i = i + 1 }
    var outer = 0
    for outer < 20 { val doubled = for x in lst { x * 2 }; outer = outer + 1 }
    val result = for x in lst { x * 2 }
    println(result.len()); println(result.get(0) or { -1 }); println(result.get(1999) or { -1 })
}
