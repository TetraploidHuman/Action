fun main() {
    // Smart cast with != null check
    val x int? = 42
    when x != null {
        print(x + 1)
    }

    // Smart cast with == null check (else branch gets smart cast)
    val y int? = 99
    when y == null { print(0) else print(y + 1) }
}
