fun main() {
    var lst = List[]; var i = 0
    for i < 2000 { lst = lst.append(i); i = i + 1 }
    var outer = 0
    for outer < 50 {
        val evens = filter(lst) { it % 2 == 0 }
        val doubled = map(evens) { it * 2 }
        val sum = fold(0, doubled) { acc, x; acc + x }
        outer = outer + 1
    }
    val evens = filter(lst) { it % 2 == 0 }
    val doubled = map(evens) { it * 2 }
    val sum = fold(0, doubled) { acc, x; acc + x }
    println(sum)
}
