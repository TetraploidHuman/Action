// Function type examples
// Higher-order function with typed parameter
fun apply_twice(f: (int) -> int, x: int): int {
    f(f(x))
}

fun double(n: int): int {
    n * 2
}

fun main() {
    val r = apply_twice(double, 5)
    print(r)  // 20 = double(double(5))
}
