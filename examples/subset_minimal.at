fun main() {
    // Subset with single element not in superset - should be false
    val r = setIsSubset(set["x"], set["y"])
    print("subset ({x}) of ({y}): ")
    print(r)
    println("")

    println("done")
}
