fun main() {
    var lst = list[]
    var i = 0
    for i < 2000 {
        lst = lst.append(i)
        i = i + 1
    }
    // single insert on 2000-element list
    var ins = lst.insert(0, 9999)
    println(ins.len())
    println(ins.get(0))
}
