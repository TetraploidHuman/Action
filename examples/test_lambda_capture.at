// Test: lambda expressions - captures, multi-param, implicit it
fun main() {
    // Capture outer variable
    val n = 100
    val f = { x -> x + n }
    print(f(20))
    print(f(50))

    // Implicit it parameter
    val d = { it * 2 }
    print(d(21))
    print(d(5))
}
