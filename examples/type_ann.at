// Type annotation examples
fun main() {
    // Basic type annotations
    val x int = 42
    print(x)

    val pi float = 3.14
    // float printing not yet supported, but the binding works

    val b bool = true
    print(1)

    // Type annotation with coercion: int literal -> float variable
    val f float = 42
    print(2)
}
