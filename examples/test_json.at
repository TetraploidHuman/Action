// Test: JSON FFI — minimal step-by-step
external type JsonNode
external fun action_json_parse(s: CString) -> Ptr[JsonNode]
external fun action_json_free(node: Ptr[JsonNode])

fun main() {
    // Step 1: Can we call action_json_parse at all?
    val raw = action_json_parse(toCString("42"))
    println(42)
    action_json_free(raw)
}
