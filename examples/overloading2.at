// Test function overloading - comprehensive
fun greet(name: string): string {
    "Hello, " + name
}
fun greet(times: int): int {
    times * 2
}

fun process(x: int): int {
    x * 10
}
fun process(x: float): float {
    x * 10.0
}

fun main() {
    // string overload
    println(greet("World"))

    // int overload
    println(greet(5))

    // int overload via identifier
    val n = 3
    println(process(n))

    // float overload
    println(process(2.5))

    // int overload with expression arg
    println(process(1 + 2))
}
