fun main() {
    var lst = List[]; var i = 0
    for i < 2000 { lst = lst.append(i); i = i + 1 }
    var outer = 0
    for outer < 50 { val result = lst.map { it * 3 }.filter { it > 1000 }; outer = outer + 1 }
    val result = lst.map { it * 3 }.filter { it > 1000 }
    println(result.len()); println(result.get(0) or { -1 })
}
