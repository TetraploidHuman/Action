// Phase 3: self field assignment must be rejected.
type Point {
    x: Int
    y: Int

    fun bump(self) -> Int {
        self.x = self.x + 1
        return self.x
    }
}

fun main() -> Int {
    val p = Point { x = 1, y = 2 }
    return p.bump()
}
