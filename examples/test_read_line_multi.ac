// Test readLine with multiple piped inputs (fallible; use or { } on EOF)
fun main() {
    var running = true
    for running {
        val input = readLine() or { "quit" }
        running = if input == "quit" { false } else { true }
        if running { println("Hello, " + input) }}
    print("Goodbye!")
}
