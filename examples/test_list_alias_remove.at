// Alias stress: lst + ins share scope; repeated remove(0) on ins must not corrupt lst.
fun main() {
    var lst = List[]
    var i = 0
    for i < 200 {
        lst = lst.append(i)
        i = i + 1
    }
    var ins = lst
    i = 0
    for i < 10 {
        ins = ins.remove(0)
        i = i + 1
    }
    println(ins.len())
    println(lst.len())
}
